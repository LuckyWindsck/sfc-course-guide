input {
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/postgresql-42.2.8.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://postgresql:5432/${INIT_DB}"
        jdbc_user => "${INIT_USER}"
		jdbc_password => "${INIT_PASSWORD}"
        jdbc_validate_connection => true
        statement => "SELECT * FROM lecture_info"
    }
}

output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        user => "${ELASTIC_USER}"
        password => "${ELASTIC_PASSWORD}"
        index => "lecture_info"
    }
}
